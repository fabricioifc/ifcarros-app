import { Dimensions, StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  // empty: { justifyContent: 'center', alignItems: 'center', height: '100%' }
  empty: { 
    flex: 1, alignItems: 'center', justifyContent: 'center', marginTop: 10
  }
});
